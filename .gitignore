*.retry
.vault_pass
*.log

# Real credentials - never commit password hashes. Keep only the .sample.json.
bootstrap/user_credentials.json

# ISO build output - large binaries, rebuildable from iso/build-iso.ps1
iso/out/
